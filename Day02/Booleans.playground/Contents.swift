import Cocoa

let goodDogs = true
var gameOver = false

print(gameOver)
gameOver.toggle() // toggle per girar el valor bool
print(gameOver)

let isMultiple = 120.isMultiple(of: 3)

var isAuthenticated = false
isAuthenticated = !isAuthenticated // ! per girar, fer el contrari
print(isAuthenticated)

isAuthenticated = !isAuthenticated
print(isAuthenticated)

