/*
 té 3 parts:
     Una condició (una pregunta que es respon amb cert o fals).
     El resultat si la condició és certa.
     El resultat si la condició és falsa.
 
 condició ? valor_si_cert : valor_si_fals
 WTF --> “what, true, false”
 
 */

let age = 18
let canVote = age >= 18 ? "Yes" : "No"

let hour = 23
print(hour < 12 ? "It's before noon" : "It's after noon")
